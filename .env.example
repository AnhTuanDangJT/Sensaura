# Sensaura (Next.js) — copy to .env.local for local overrides.
# Vercel: set these in Project → Settings → Environment Variables if needed.

# Optional: when you connect the Spring Boot API (ai-backend), set the public base URL:
# NEXT_PUBLIC_API_URL=https://your-api.example.com
